# JARVIS v14 Ultimate - Python Dependencies
# ==========================================
# Device: Realme 2 Pro Lite | RAM: 4GB | Platform: Termux

# Core Dependencies (Required)
click>=8.0.0
colorama>=0.4.0
python-dotenv>=0.19.0
pyyaml>=6.0
requests>=2.26.0
tqdm>=4.62.0
schedule>=1.1.0
typing-extensions>=4.0.0

# Enhanced UI/Logging
rich>=12.0.0
loguru>=0.6.0

# Security & Encryption
cryptography>=3.4.0
# pynacl is OPTIONAL - will use fallback if not available
# On Termux, install with: pkg install libsodium && pip install pynacl
# If pynacl fails, JARVIS will still work with cryptography fallback

# HTTP Client
httpx>=0.24.0

# System Monitoring (Termux compatible)
psutil>=5.9.0
